<%= form_for(@sunami) do |f| %>
  <% if @sunami.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sunami.errors.count, "error") %> prohibited this sunami from being saved:</h2>

      <ul>
      <% @sunami.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :box %><br>
    <%= f.text_area :box %>
  </div>
  <div class="field">
    <%= f.label :time %><br>
    <%= f.datetime_select :time %>
  </div>
  <div class="field">
  <%= f.label :category_id %><br>
  <%= f.collection_select(:category_id, Category.all, :id, :name) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
